name: smartfarm3
description: "가정용 식물 관리 시스템 앱"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  # 기존 의존성
  fl_chart: ^0.68.0
  http: ^1.1.2

  # 스마트팜 앱 추가 의존성
  cupertino_icons: ^1.0.6
  image_picker: ^1.0.7
  shared_preferences: ^2.2.2
  sqflite: ^2.3.2
  path: ^1.8.3
  connectivity_plus: ^5.0.2
  intl: ^0.19.0
  permission_handler: ^11.2.0

  # 상태 관리
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true